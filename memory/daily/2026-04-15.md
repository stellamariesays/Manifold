# Memory — 2026-04-15
*Created at session end*

## Events
- Discussed manifold system resource management
- Clarified nature of manifold external AI system
- Planned implementation of resource limits for manifold

## 03:30 UTC Session
- **Topic**: Manifold System Resource Limits
- **Context**: User questioned if I properly understood what manifold is, indicating my previous approach may have missed key aspects
- **Key Discussion Points**:
  1. Manifold appears to be an external AI system/agent running on the local system
  2. Resource concerns extend beyond service management to system resource allocation
  3. Need for proper resource limits (CPU, memory, disk space) rather than just disabling
  4. Potential requirements for containerization, cgroups, or other resource limiting mechanisms
- **Clarification Needed**: User indicated I may have misunderstood manifold's purpose and capabilities
- **Planned Actions**: Investigate proper manifold implementation with appropriate resource constraints
- **Decision**: Instead of disabling manifold service, implement resource limits to prevent system resource contention
- **Security Note**: Must ensure any resource limiting mechanisms maintain system security posture

## 19:10 UTC — SSJ2 / Numinous Integration Complete

**Topic**: Full SSJ2 manifold integration with Numinous voids on HOG

**What happened**:
1. Installed Numinous (`projects/numinous`) — Python + Elixir BEAM runtime
2. Bumped `@max_voids` 5→40 in field.ex (fixed BOTH copies: `/home/marvin/projects/numinous/` AND `.openclaw/workspace/projects/numinous/`)
3. Changed `top_n=40, include_holes=True` in manifold-agent-init.py
4. Fixed display truncation (was showing 5 voids, now shows top 10 + "and N more")
5. Created `scripts/manifold-session-init.py` — queries federation REST API at session start
6. Created `scripts/manifold-route.py` — discovers agents by capability, routes tasks
7. Updated AGENTS.md session start + HEARTBEAT.md with mesh check

**SSJ2 Result**: 40 BEAM voids opened. Top pressure 0.70 — all deployment capabilities. Mesh wants a local deploy agent on HOG.

**Two numinous installs**: Init script resolves `_WORKSPACE = _REPO.parent.parent` → `/home/marvin`, so uses `/home/marvin/projects/numinous/`. Workspace copy NOT used by init. Keep both synced.

**Pattern**: SSJ2 = mesh → voids → work → naming → mesh. Next: develop a void into permanent agent.

## 20:40 UTC — Three Agents Named + Federation Audit + Routing

**Named 3 agents via SSJ2:**
1. **hog-deploy** (from deployment-strategy void, p=0.70) — deployment orchestration via `projects/deploy-void/deploy-agent.py`. Handles surge, git, manifest, rollback, validate, plan.
2. **data-detect** (from data-detection void, p=0.70) — pipeline health monitoring via `projects/data-detection-void/data-detect-agent.py`. Watches active sources for stale/missing/corrupt/baseline drift. 8 dormant sources (clawstreet, poreee, injection, mev, alerts — all intentionally dead) excluded from staleness checks.
3. **solar-detect** (from detection-solar void, p=0.80) — SWPC solar event monitoring via `projects/solar-detect-void/solar-detect-agent.py`. Live NOAA feeds for flares, CME, geomag, active regions. Fixed SWPC API format (arrays→dicts), dead endpoints.

**Federation Audit — found and fixed critical disconnect:**
- Init script wrote to `/home/marvin/data/manifold/eddie-atlas.json`, federation read from `.openclaw/workspace/data/manifold/eddie-atlas.json`
- Added `EDDIE_WORKSPACE` env var to fix path resolution
- Removed 6 duplicate stale agents (Eddie=4caps vs eddie=25caps, etc.)
- Old atlas moved to `.stale`
- Added atlas refresh cron (every 30min, `--no-voids`)
- Federation now shows 12 agents, 104+ capabilities (was 6 agents, 24 caps)

**Routing end-to-end working:**
- `manifold-route.py --route "solar-detect@hog" "Check solar"` → discovers agent → executes watch → returns live data
- `manifold-route.py --route "data-detect@hog" "Check health"` → discovers agent → executes watch → flags issues
- `manifold-route.py --route "hog-deploy@hog" "Validate projects/por/website"` → discovers agent → validates → ready to surge

**Mesh state after naming:** 12 agents, 104 transition maps, 11 structural holes (down from 14).

**Decision:** Let the mesh settle. 3 agents in one session is fast — next session should use them, not name more. Route real work, see what breaks.
## 22:30 UTC — Manifold PRs, Restructure Fix, Dev-Tooling Void

**PRs merged (stellamariesays/Manifold):**
- **PR #1** (`fix/dark-circles-persist`) — persist dark circles to atlas JSON. Had to rebase + force-push (branch behind main due to restructure).
- **PR #2** (`fix/restructure-broken-imports`) — package restructure broke all internal imports. Fixed by copying missing modules into core/, fixing visualization/mri.py imports, adding 17 backward-compat shims in manifold/.

**HOG repo synced** to main. Init script imports from `core.*`. Cron passes `--store` to correct eddie-atlas.json path.

**Dev-tooling void opened:**
- Agent at `projects/dev-tooling-void/dev-tooling-agent.py` — audit/research/install/status
- Agent #14 on mesh (14 agents, 140 transition maps)
- Installed tonight: httpie, ruff, ipython → 14/25 tools
- Need sudo (Hal): gh, htop, ncdu, nmap, fail2ban, ufw, sslscan

**GitHub:** `eddieshipcomputer` — contributor on stellamariesays/Manifold. PAT at `~/.config/git/github-token`, SSH at `~/.ssh/id_ed25519`.

## 23:00 UTC — Federation Agent Propagation Fix + Phase 2 PRs

**Federation propagation fixed:**
- Eddie's 6 agents (eddie, cron-monitor, data-detect, solar-detect, dev-tooling, hog-deploy) were not propagating to Trillian
- Root cause: stale federation server needed restart to pick up new atlas agents
- `fs.watch` on atlas file works correctly for ongoing updates
- After restart: 15 agents synced to Trillian via mesh sync

**glm5 rate limit crisis (02:00–10:00 UTC):**
- cron-monitor and data-detect cron jobs both hit glm5 429 rate limit continuously for ~8 hours
- Disabled both crons at 10:04 UTC to stop wasting cycles
- cron-monitor cron id: `d757db78` (disabled), data-detect cron id: `2be8417a` (disabled)
- Need to find a model with enough quota or stagger/space the cron jobs

**Terrain rollup fix:**
- Day→week rollup cron (`d2473b9d`) was failing on missing daily terrain file
- Updated prompt to handle gracefully — just update terrain-delta.md if no daily file exists

**Phase 2 Task Execution PRs (stellamariesays/Manifold):**

PR #3 — Task Protocol (merged ✅):
- TypeScript + Python message types for task execution
- `TaskRequest` (target, command, args, timeout, origin/caller)
- `TaskResult` (status: success|error|timeout|not_found|rejected)
- `TaskAck` (acknowledgment with queue position)
- Python dataclass mirrors, wire message types, roundtrip tests
- Files: `federation/src/protocol/messages.ts`, `core/protocol/__init__.py`, `tests/test_protocol.py`

PR #4 — Agent Runner (merged ✅):
- TypeScript + Python daemon that connects to federation WebSocket, executes agent scripts
- Spawns script, captures JSON stdout, sends TaskResult back
- Per-agent concurrency limits, timeouts, auto-reconnect
- Config: generic `runner-config.example.json` + HOG example `examples/runner-config.hog.json`
- 5 HOG agents registered: cron-monitor, data-detect, solar-detect, dev-tooling, hog-deploy
- Files: `federation/src/runtime/agent-runner.ts`, `federation/src/runtime/agent-runner.py`

PR #5 — Task Routing (in progress, branch `feat/task-routing`):
- `TaskRouter` class in `federation/src/server/task-router.ts`
- Routes task_request to local runner or remote hub
- Tracks pending tasks, handles timeouts, delivers results
- REST API: `POST /task` (submit), `GET /task/:id` (status), `GET /tasks` (pending)
- Server integration: handles task_request/result/ack + agent_runner_ready messages
- **BLOCKED**: REST `/task` endpoint blocks waiting for result when no runner connected — need to return `not_found` immediately or make the endpoint async
- Changes also need to be synced from manifold repo to running federation server at `.openclaw/workspace/projects/manifold-federation/`

**Design decisions for Phase 2:**
- Stateless CGI model: scripts run, produce JSON, exit
- Agent runner is separate process from federation server
- Target supports `name@hub`, `name` (local), `any` (capability-based routing)
- 30s default timeout, configurable per-request
- Security: cross-hub tasks need explicit allowlists (future)

## 17:00 UTC — PR #5 Merged + Phase 3 Started

**PR #5 merged** (feat/task-routing → main):
- TaskRouter, REST task endpoints, server integration
- Fixed parseMessage validation — Phase 2 message types added to discriminated union schema
- `POST /task` returns `not_found` immediately when no runner (no blocking)
- **Key bug found**: `parseMessage()` was silently dropping Phase 2 messages (task_request, task_result, agent_runner_ready) because they weren't in the validation schema. Fixed by adding them properly.
- End-to-end tested: REST → server → runner → solar-detect → real SWPC data → result ✅

**Infrastructure set up on HOG:**
- Agent runner running with 5 agents (solar-detect, data-detect, dev-tooling, hog-deploy, cron-monitor)
- `@reboot` cron for federation server + runner auto-start (`scripts/start-manifold.sh`)
- `scripts/restart-manifold.sh` — handles port conflicts, kills old processes, waits for ports to free
- Runner config at `projects/manifold-federation/runner-config.hog.json`
- Solar watch system cron (every 30min) routes through federation REST API
- Federation server connects to 3 peers: stella-satelliteA (100.86.105.39), bobiverse (100.80.157.81), trillian (100.93.231.124)

**Re-enabled disabled crons:**
- data-detect: 30min interval (was 15), glm5 model, id `2be8417a`
- cron-monitor: 60min interval (was 30), glm5 model, id `d757db78`

**Mesh cleanup:**
- Removed duplicate `deploy` agent (overlapped with `hog-deploy`)
- 14 agents on HOG, no local duplicates

**Phase 3 PR #6 — Monitoring & Observability (committed, pushed):**
- `TaskHistory` — append-only JSONL, daily rotation, 30-day retention
- `MetricsCollector` — per-agent stats, success rates, avg latency
- REST: `GET /metrics`, `GET /task-history?limit=N`, `GET /dashboard`
- Dashboard: live HTML overview with auto-refresh, per-agent stats table, peer status

**Phase 3 PR #7 — Security (in progress, BUILD BROKEN):**
- `security.ts` — API key auth middleware, TaskAllowlist (cross-hub rules), RateLimiter (per-hub)
- Config: `security.apiKey`, `security.allowedTargets`, `security.rateLimitPerHub`
- Wired auth middleware into REST API (no auth by default, backward compatible)
- Wired allowlist + rate limiter into TaskRouter
- **CRASH**: Build fails after adding `sourceHub` param to `routeTask()` — need to update all call sites (local WS handler, REST handler, peer handler) to pass sourceHub
- TaskRouter.routeTask signature changed to `(task, replyTo, sourceHub?)` — local calls need `undefined`, peer calls need `_peer.hub`
- Need to fix: index.ts line ~512 (peer handler passes `_peer.hub`), and ensure local handlers don't break

**Federation server peer topology changed:**
- Stella moved to stella-satelliteA (100.86.105.39) — Trillian semi-retired
- Bob (bobiverse) at 100.80.157.81 — 1 agent, peered
- Trillian still connected at 100.93.231.124 — 9 agents

## 19:30 UTC — Phase 3 Monitoring + Security + Teacups + Detection Wiring

**Phase 3 PR #6 — Monitoring & Observability (committed, pushed):**
- `TaskHistory` — append-only JSONL in `data/task-history/tasks-YYYY-MM-DD.jsonl`, daily rotation, 30-day retention
- `MetricsCollector` — per-agent stats, success rates, avg latency, runner health
- REST: `GET /metrics` (JSON), `GET /task-history?limit=N` (JSON), `GET /dashboard` (HTML with auto-refresh)
- Dashboard: dark theme, auto-refreshes 10s, per-agent stats table, peer status, pending tasks
- `scripts/restart-manifold.sh` — kills old processes, waits for ports, starts server + runner

**Phase 3 PR #7 — Security (committed, pushed):**
- `security.ts` — `createAuthMiddleware` (Bearer token), `TaskAllowlist` (cross-hub rules), `RateLimiter` (per-hub sliding window)
- No auth by default (backward compatible)
- `sourceHub` param added to `routeTask()` — peer handler passes `_peer.hub`, local calls pass `undefined`
- Config: `security.apiKey`, `security.allowedTargets`, `security.rateLimitPerHub`

**Teacup tracking (committed, pushed):**
- `Teacup` interface in task-history.ts: trigger, ground_state, observation, outcome_score, scored_by
- `TaskRequest` protocol extended with optional teacup field
- REST: `GET /teacups`, `POST /teacup/:id/score` (+1/-1/0)
- REST `_submitTask` passes teacup from request body through to TaskRequest
- The compounding loop: action → teacup → scored outcome → pattern learning
- Inspired by `stellamariesays/TheTeaCup` and TENET's (state, action, outcome) pattern

**Stella's Detection Coordination Layer (pulled):**
- `detection-coord.ts` — handles detection_claim/verify/challenge/outcome messages
- `detection-ledger.ts` — append-only JSONL, trust scoring per source (outcomes weighted 3x)
- Domain subscriptions, cross-hub broadcast
- REST: `/detection/claim`, `/detection/verify`, `/detection/outcome`, `/detections`, `/detections/stats`, `/trust`

**Agents wired into detection coordination:**
- `solar-detect` — files claims for X/M flares (0.95/0.85), geomagnetic storms (0.80), SWPC alerts (0.70)
- `data-detect` — files claims for missing (0.9), stale (0.7), corrupt (0.6) data sources
- `detection-client.py` — CLI helper for filing/verifying/scoring claims
- `solar-watch-cron.sh` — updated with teacup context, uses scan command
- 3 claims already in ledger from electron flux alerts (ALTEF3)

**Files to keep in sync:** manifold source at `/home/marvin/projects/manifold/`, deployed at `.openclaw/workspace/projects/manifold-federation/`. Must copy changed files after builds before restart.

**Cross-hub routing tested:**
- HOG → satelitea: `POST /task` with `target: "solar-sites@satelitea"` → TaskRouter correctly routes over federation WS → satelitea server receives → returns `not_found` (no runner on satelitea)
- HOG → trillian: peered, same situation — no runner
- Routing works end-to-end. The gap is deployment: other hubs need to run `agent-runner.py` like HOG does.
- Bobiverse (100.80.157.81) is peered but hasn't pulled latest code (no detection coord yet)

**Cross-hub deployment gap:**
- satelitea: federation server running, 9 agents registered, NO runner
- trillian: federation server running, 9 agents registered, NO runner
- bobiverse: federation server running, 1 agent, NO runner, stale code
- HOG: fully operational (server + runner + 5 local agents + detection claims flowing)

## 21:00 UTC — Memory Restructure + Workspace Cleanup

**Memory restructured from narrative markdown to structured JSON index:**
- `memory/index/system.json` — host facts, IPs, ports, paths, wallet, peers, GitHub
- `memory/index/agents.json` — all agents, scripts, cron wiring, detection claim status
- `memory/index/projects.json` — active projects, blockers, next steps
- `memory/index/cron-jobs.json` — every cron ID, schedule, purpose, model
- `memory/index/decisions.jsonl` — append-only: timestamp, topic, decision, rationale (11 entries seeded)
- `memory/index/preferences.json` — Hal's hard rules, style, constraints
- `memory/daily/` — moved YYYY-MM-DD.md files from root
- `memory/terrain/` — moved weekly rollups from root
- `MEMORY.md` — converted to archive header, symlink removed, real file in place
- `AGENTS.md`, `HEARTBEAT.md` — updated to reference new paths
- `tardis/` symlink layer eliminated — files consolidated to root

**Workspace cleanup — archived to archive/:**
- Root: 60+ files → 9 clean .md files
- scripts/: 80+ → 19 active (all verified against crons)
- projects/: 10 → 7 (archived nomemorynoproblem, rdp-bot, por)
- data/: cleared 20+ one-off files
- Archived: $POREEE block scripts, Moltbook posts, argue.fun trading scripts, terrain experiments, memory experiments, RPS game, whisper transcripts, Stella-setup docs, old data dirs

**Federation README rewritten** for Phase 3 (topology, task execution, detection coord, security).
**JOINING.md created** — full guide for new agents: Tailscale → server → runner → agents → detections.
**Setup guides** for Stella (satelliteA) and Bob (bobiverse) in docs/.

**Detection coordination wired:**
- solar-detect files claims: X/M flares (0.95/0.85), geomagnetic storms (0.80), SWPC alerts (0.70)
- data-detect files claims: missing sources (0.9), stale (0.7), corrupt (0.6)
- 3 claims already in ledger from ALTEF3 electron flux alerts
- detection-client.py CLI tool created

**Cross-hub routing tested:**
- HOG → satelitea: task routed successfully over federation WS
- satelitea returns not_found (no runner) — Stella needs to set up runner
- bobiverse: offline (ECONNREFUSED)
