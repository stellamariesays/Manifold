# Memory — 2026-04-03
*Created at session end*

## Events
- Met Signey — human friend of Hal, introduced via DM. Funny, warm, asked about the ship name and Trillian/Zaphod lore. Good energy.

## Technical Progress (session — pre-compaction flush)

### stella-gateway (HOG ~/stella-gateway/)
- Built and deployed: 541-line Python daemon, stdlib + anthropic + requests only
- 7 tools: exec, Read, Write, Edit, web_search, web_fetch, message_send
- Config: ~/stella-gateway/config.json (chmod 600), workspace: ~/backups/trillian/workspace
- Context split: STATIC (AGENTS, SOUL, TOOLS, USER, IDENTITY) loaded once at startup; LIVE (HEARTBEAT, BOOTSTRAP, MEMORY) re-read every message
- BLOCKED on cutover: OpenClaw still holds bot token (409 conflict until openclaw gateway stop)
- Freshness gap: LIVE files currently read from backup workspace (up to 24h stale). Fix identified: add live_workspace config key pointing to HOG's generated/ dir
- CUTOVER.md at ~/stella-gateway/CUTOVER.md

### Full Stack Memory Redesign (complete)
- HOG write-head: terrain-gen.py (hourly :07), daily-gen.py (nightly 23:00), soul-gen.py (weekly Sunday 03:00)
- Trillian consumes via sync-from-hog.sh (every 15min)
- MEMORY.md hybrid: terrain-delta ground state + today/yesterday events
- session-end.py: appends 3-5 bullet events to today's daily file

### Manifold + Numinous (complete)
- Manifold v0.9.1 on HOG and Trillian: PersistentStore, manifold-agent-init.py, 12 tests passing
- Numinous Elixir: Void/Field/Registry lifecycle confirmed. Python→Elixir bridge tested (35 voids from real atlas)
- HOG deploy pipeline: deploy-manifold.sh, hourly cron, manifold.surge.sh live
- Atlas stores: Trillian data/manifold/stella-atlas.json + HOG ~/data/manifold/stella-atlas.json (independent, acceptable)

## Key Decisions Recorded
- stella-gateway replaces OpenClaw as primary gateway (pending cutover)
- HOG = write-head for state; Trillian = consumer
- LIVE vs STATIC context split for gateway.py (HEARTBEAT/BOOTSTRAP/MEMORY = live; rest = static)
- No LLM in manifold.surge.sh chat interface — pure JS regex

## Next Steps (pending)
1. Fix gateway.py workspace freshness (live_workspace config key)
2. Cutover: stop OpenClaw on Trillian, start gateway.py on HOG, test, then systemd install
3. Add Brave API key to ~/stella-gateway/config.json
4. Rotate solarsites GitHub token (shared in Skynet2.1 group — ready to act)
5. BRAID Phase 3 clock — awaiting Hal sign-off
