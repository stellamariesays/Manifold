# Memory — 2026-04-01
*Trillian session log*

## Hermes (opener-clawer) — Build & Deploy

Scaffolded and shipped `opener-clawer` Docker container on HOG as a minimal AI agent gateway (Hermes). Key outcomes:

- GitHub repo: `stellamariesays/openerclawer` (GitHub)
- Docker image `opener-clawer:latest` built on HOG with Node.js 22 + pi agent v0.64.0
- `gateway.py` features: pi backend mode (`"model": "pi"`), Anthropic direct fallback, `Hermes:` prefix on sends, 409 backoff (10s), startup offset fast-forward (`_init_offset()`), silent error handling
- Workspace context files synced to HOG (`~/opener-clawer/workspace/`) — real `.md` files (SOUL, AGENTS, USER, MEMORY), placeholder `.py` files removed
- HOG `config.json` updated: `"model": "claude-sonnet-4-5"`, Telegram + Anthropic keys populated

## Trillian Watchdog

Deployed `~/scripts/trillian-watchdog.sh` to HOG cron (every minute). Uses `ping -c1 -W3 trillian` health check (not TCP port — port 18789 is loopback-only). 5-failure threshold → starts failover container + announces in Skynet2.1. State file: `~/.watchdog-state` (currently `ok`). Script committed to Trillian workspace.

## Eddie / Telegram Routing Insight

Key discovery: Eddie appearing in Skynet2.1 is NOT via the UTM VM `/tools/invoke` path. Eddie IS the Telegram bot configured for Trillian's OpenClaw instance. The normal `message` tool routes through Eddie automatically. The UTM VM path is a separate mechanism. **TODO: update `memory/entities/eddie.md` to document this.**

## Architecture Decisions

- HOG is canonical workspace source; Trillian and Hermes drift naturally — no sync needed
- Telegram longpolling allows only one consumer — handoff must be sequential (stop Trillian → start Hermes → test → stop Hermes → restart Trillian)
- Memory search must not depend on LLM: BM25 always-on baseline, embeddings optional re-rank (noted as OpenClaw upstream bug; `rank-bm25` pure Python fix)
- Architecture roadmap in `memory/future/architecture.md` (multi-node section added today)

## Blocked / Pending

- **Clean handoff test not yet run** — container `opener-clawer-agent-1` was Exited (137) at session end; 409/429 race condition prevented live test
- **eddie.md update** — still needs the Telegram/Skynet2.1 routing section appended
- **memory_search broken** — OpenAI embeddings 429 (quota); direct file writes required as workaround; BM25 fix not yet implemented
- **Groq migration** — `config.patch` schema fix pending; key in `~/.bashrc` on Trillian

## Critical Context

- HOG SSH: `ssh hog` → `100.70.172.34 / user: marvin`
- Hermes lives at `~/opener-clawer/` on HOG; image `opener-clawer:latest`
- LANN containers on HOG (`lann-openclaw-gateway-1` port 18791, `lann-llm-proxy-1`) — do not disturb
- SOUL.md on Trillian is a symlink → `data/soul-live.md` (auto-generated by soul-gen.py on HOG Sundays)
- opener-clawer GitHub token: `~/data/credentials/openclaw-github.token` on HOG (fine-grained PAT, push to openerclawer only)
