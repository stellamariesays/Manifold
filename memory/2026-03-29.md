## 2026-03-29

### FOG repo — Elixir + Haskell implementations

Built Python (reference) + Elixir (service shape) + Haskell (types-as-spec) implementations of FOG. All three cover FogMap, FogDelta (diff), FogSeam (measure), arbitrage detection.

Pushed to `github.com/stellamariesays/fog`. Added `fog-implementations` as git submodule to Manifold.

Hal's call: build both and put them in the README so people can pick.
Manifold integration: submodule only. Python fog stays embedded. Elixir/Haskell service promotion deferred until mesh goes distributed.

---

### Groq migration — in progress (13:42 WITA)

Hal chose option 2: move off Anthropic → Groq as primary model provider.

**Status:** Partially complete. Blocked on config.patch rejecting apiKey in auth profile ("invalid config").

**What's done:**
- Key saved to `data/credentials/groq.token`
- `GROQ_API_KEY` export added to `~/.bashrc`
- Config patched: `groq:default` auth profile added, primary model set to `groq/llama-3.3-70b-versatile`, fallbacks: `anthropic/claude-sonnet-4-6`, `openai/gpt-4o-mini`
- Gateway restarted (SIGUSR1) — but GROQ_API_KEY not in gateway process env yet

**Blocker:** `config.patch` with `apiKey` field in auth profile returns "invalid config". Need to find correct way to inject key — either via env var (needs full process restart to pick up .bashrc) or correct config schema field name.

**Next session:** Resume from here. Try full `openclaw gateway restart` to pick up env var, or check config schema for correct apiKey field name in auth profiles.

**Security note:** Groq key `gsk_jlpAb47...` was shared in Skynet2.1 group chat (message 9295). Should be rotated after migration is stable. Add to pending tasks.
