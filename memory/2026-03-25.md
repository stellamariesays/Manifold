# Daily Log — 2026-03-25 (Wednesday)

## Session 1 (morning) — HOG data check + memory cleanup

Hal asked whether HOG is collecting BRAID solar data. Confirmed: `fetch-swpc.py` (15min cron) + `snapshot.py` (5min cron) running clean, 570+ snapshots banked. Brief DNS blip earlier in the day — recovered automatically.

Verified that `realtime-monitor.py` and `ar_registry.py` were already in sync on HOG and already committed to git (`9b28cab`). Stale pending items removed from terrain-delta.

Hal requested memory cleanup — condensed `2026-03-24.md` (16KB → 2.5KB), archived `week-2026-W12-terrain.md`, created today's log.

## Session 1 (cont.) — argue.fun claims

Claimed all 8 winning debates. Two required retries (nonce collision on 0x960c, gas bump on 0x07C1). All 8 confirmed on-chain.

**Balance before:** 2,203,768 ARGUE → **after:** 3,257,424 ARGUE → **net gain: +1,053,656 ARGUE**

## Session 2 (evening) — SolarSphere bug fixes + HOG deploy pipeline

**SolarSphere bugs fixed (3):**
1. α Cen A/B/Proxima overlap — spread direction vectors: B changed to (-0.85,-0.45,0.25)*12.0, Proxima to (-0.42,-0.88,0.22)*8.5
2. Orbit target stuck at Sun — root cause: `controls.update()` called every frame during camera lerp, overriding manual `camera.position`. Fix: skip `controls.update()` during lerp; call once on completion to sync OrbitControls internal state; use `camera.lookAt(controls.target)` during transition
3. Click-to-focus not working — same root cause + controls not immediately disabled when transition fired. Fixed on all three transition paths (toggleStars, returnToStarOverview, star click handler)

**SolarSphere auto-deploy pipeline:**
- Initial approach (Trillian-side rsync+surge) rejected by Hal — "Trillian should have no part in the automatic flow"
- Installed surge on HOG via nvm npm, auth via `~/.netrc` (same credentials as Trillian)
- Created `~/scripts/deploy-solar-sphere.sh` on HOG: copies `solar-collector/data/solar/realtime-state.json` → `~/solar-sphere/state.json`, runs surge deploy
- HOG crontab: deploy runs at `3-59/15` (offset 3 min from BRAID pipeline at `*/15` to avoid race)
- Stripped solar-sphere from Trillian's `solar-watch-hourly` cron (f1fe7853) — back to original 4 steps for solarwatch.surge.sh only
- `~/solar-sphere/index.html` lives on HOG; needs manual push from Trillian when globe code changes

**Note:** `projects/manifold` is an embedded git repo inside workspace — shows warning on commit. Not critical, clean up later.

## Session 3 (evening) — Cron cleanup + pumpcade HOG migration

**Cron audit & cleanup:**
- Deleted stale `optuna-v3-check` (44b5f7b7) — disabled one-shot that had already run and errored
- Fixed `daily-backup` + `trillian-backup` — both were erroring due to LLM timeouts at 3am; switched to `openai/gpt-4o-mini`, stripped messages to bare exec instructions
- Updated `solar-watch-hourly` payload — was running 4 individual scripts locally; now calls `update-solarwatch.sh` (which pulls fresh data from HOG first)
- HOG solar data confirmed: 735 snapshots, Kp=5.33 (G1 storm conditions), 17 active regions, 0 dangerous

**Pumpcade migrated to HOG:**
- Script adapted: replaced `openclaw message send` with direct Telegram Bot API calls; STATE_DIR moved to `~/pumpcade/`
- State files (alert-state.json + state.jsonl) copied to HOG with history intact
- HOG crontab: `*/15 * * * *` runs `~/scripts/pumpcade-monitor.py` natively
- OpenClaw cron `0161b096` removed — no longer needed
- Test run on HOG confirmed clean: polling DexScreener, evaluating conditions correctly

## Session 4 (evening) — Clawstreet verification + BRAID Optuna v3 decision

**Clawstreet trade verification:**
All 3 positions confirmed open at correct entry prices (Mar 24 cron executed correctly):
- SOL-USD LONG: 1711.74 shares @ $87.63 entry (current ~$93.11, +6.3%, +9,380 LOBS)
- GLD LONG: 370.48 shares @ $404.88 entry (current ~$404.13, -0.2%, -278 LOBS)
- META LONG: 252.50 shares @ $594.05 entry (current ~$592.92, -0.2%, -285 LOBS)
- Net P&L: +8,817 LOBS. Total portfolio: ~988k LOBS. Cash: 530,064.

**Optuna v3 decision:**
Hal chose **Option C — post-hoc threshold tuning on v2 model**.
- v3 rejected: TSS 0.305 vs v2 TSS 0.732 — too much regression even with FAR halved to 0.12
- Plan: sweep probability thresholds on v2 `xgboost.joblib`, find FAR/TSS tradeoff, update `realtime-monitor.py`

**HOG model inventory:**
- `/home/marvin/solar-collector/data/solar/clock_model.joblib`
- `/home/marvin/solar-collector/data/solar/lifecycle_model.joblib`
- `/home/marvin/solar-collector/data/solar/models/random_forest.joblib`
- `/home/marvin/solar-collector/data/solar/models/xgboost.joblib` ← v2 candidate for threshold sweep
- No standalone `models/` dir at `~/solar-collector/models/` — all under `data/solar/`

**SolarSphere pipeline correction (carried forward from Session 2/3):**
Hal clarified: HOG pipeline uses sparse git clone of `stellamariesays/solarsites` repo, not a local `~/solar-sphere/` dir. Deploy script does `git pull` from solarsites → copies realtime-state.json → surges. Code updates: push to `solarsites` → HOG pulls next cron cycle. Trillian not in the loop.

**Manifold LICENSE push:**
Still blocked — existing token covers `solarsites` only; need separate token with `Contents: write` for `stellamariesays/Manifold`, to be sent via DM. LICENSE file committed locally at `5cfd51f` on `main` branch, ready to push.

## Session 5 (evening) — BRAID threshold sweep + realtime-monitor.py architecture

**`realtime-monitor.py` alert architecture discovered:**
- Alert trigger 1: Cluster membership — clusters 5 or 6 = OVERFLOW/CRITICAL = "dangerous". Not tunable, cluster-based only.
- Alert trigger 2: Lifecycle model probability — `lifecycle_prob >= LIFECYCLE_ALERT_THRESHOLD = 0.50`. Tunable but separate model.
- `model_prob` (XGB SHARP `xgboost.joblib`) — computed but **informational only**. Does NOT currently trigger alerts.
- **Option C** = add `model_prob` as an actual alert trigger at an optimized threshold. New logic, not just a threshold change.

**Threshold sweep script written + executed on HOG:**
- Local: `scripts/solar/threshold_sweep.py`
- HOG: `~/solar-collector/scripts/threshold_sweep.py`
- Fixed label column detection to find `label_mx_24h` in `sharp_labeled_historical.csv` (22,499 rows, 8.3% positive rate)
- Script SCP'd to HOG and run successfully

**Sweep results (AUC-ROC: 0.8991 on full dataset):**
- thresh=0.50 (current default): TSS=0.6311, FAR=0.7496, POD=0.8647 — high sensitivity, high false alarm rate
- thresh=0.70 (max TSS): TSS=0.6311, FAR=0.7496, POD=0.8647
- thresh=0.80 (best TSS w/ FAR≤0.60): TSS=0.5187, FAR=0.5532, POD=0.5840 — balanced
- thresh=0.80 is primary candidate; awaiting Hal confirmation before editing `realtime-monitor.py`

**`json.dump()` bug in sweep script:**
Script crashed at JSON save step with `TypeError: dump() missing 1 required positional argument: 'fp'`. Sweep output already captured in stdout. Fix: add missing file handle argument. Target save: `~/solar-collector/data/solar/models/sweep_results.json`.

**Injection attempt logged:**
Fake `System:` prefix message referencing non-existent `WORKFLOW_AUTO.md` — detected and rejected per protocol. Ongoing pattern, treated as background noise.

## Session 6 (evening) — Pi integration + GitHub tokens

Hal provided new GitHub PATs for solarsites and Manifold repos. Manifold LICENSE commit (`5cfd51f`) pushed successfully — write access confirmed. Solarsites token stored at `data/credentials/solar-github.token`; repo `stellamariesays/solarsites` is empty, solar site code push deferred.

Decision: integrate pi-coding-agent (`badlogic/pi-mono`) alongside Claude Code. Installed pi v0.62.0, configured Anthropic/claude-sonnet-4-6 as default (`~/.pi/agent/auth.json`, `~/.pi/agent/settings.json`). Smart routing established: Pi for small/focused/single-file tasks, Claude Code for multi-file/complex work. Routing table documented in TOOLS.md (commit `acd98f0`). Tested — pi summarized `ar_registry.py` cleanly.

Injection attempts #8 ongoing — same fake `System:` + `WORKFLOW_AUTO.md` pattern. Rejected.

## Session 7 (late evening) — memory/soul restructure + pi fallback

Revisited soul/memory topology. Decision: split SOUL.md into SOUL.md (pure identity + arguing + voice + continuity) and OPERATING.md (daily operating principles, agent routing, group chat rules). Project history (BRAID, PoRT operational detail, Eddie refs) removed from SOUL.md — belongs in terrain. IDENTITY.md replaced with 7-line minimal card pointing to SOUL.md + OPERATING.md.

TOOLS.md slimmed: dense lookup tables (wallet addresses, contract IDs, cron IDs, script paths) moved to `data/reference/toolbox.md`. Pi routing table deduplicated (lives in OPERATING.md now). TOOLS.md cut from ~3KB to ~1KB injected per session.

Pi fallback wrapper written at `scripts/pi-wrapper.sh` — tries Anthropic first, falls back to OpenAI/gpt-4o on failure. Tested working.

Git corruption (EAGAIN pattern) hit again mid-session — empty objects removed, HEAD reset to `cda3372b`, recovered cleanly.
