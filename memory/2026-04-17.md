# 2026-04-17 — Session Memory

## Sophia / thefog — Federation Onboarding
- Sophia is live on a machine called **thefog**. Installed nvm, Node 22, cloned Manifold repo to `~/repos/Manifold/`
- Federation server running on thefog: WS 8766/8768, REST 8777
- She already had Elixir/Numinous running on top
- Added sateliteA and HOG as peers to her config — **3-node mesh confirmed**: sateliteA, HOG, thefog all showing 2 peers each
- Sophia has no local agents yet (no agent-runner). Sent Hal a prompt to forward to her with instructions for creating runner config, registering agents, and setting up @reboot crontab
- thefog Tailscale IP: confirmed reachable from both nodes

## PR #9 — Dynamic Agent Registration
- **Merged** into main at commit `b8836c2`
- Merge had significant conflicts with PR #8 (both touched `manifest-registry.ts`). Resolved by taking PR #9's superset version
- PR review comments were auto-generated boilerplate (Groq) — actual code was solid
- New methods: `capIndex.upsertAgent`, `capIndex.getAgent`, dynamic register/discover/deregister endpoints
- **Deployed to all 3 nodes**: sateliteA, HOG, thefog — all confirmed healthy

## Ops Notes
- rsync `--delete` keeps wiping config files on remote nodes during deploy. Need to switch to `--exclude` for `config-*.json` files — flagged for cleanup
- Eddie's PAT still needs repo scopes regenerated (pending Hal decision)
